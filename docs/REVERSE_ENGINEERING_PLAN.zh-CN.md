# Tekla DB1 完整逆向路线图

本文档是持续更新的工作记录。每发现一个新版本、表布局或语义边界，都要先修改本文档，再扩大实现范围。

## 2026-09-27：起拱构件与独立曲线证据（已实现本地验证，加工后变形继续逆向）

对固定语料65份主库/组件库扫描，非零PartCambering共12条，来自同一7.30模型的两份副本，只有6个独立D20对象。它们无轮廓/切割操作，当前OCCT错误地生成直杆。独立IFC为6/7/8个八边形截面环，中心线圆拟合残差小于0.000009毫米。参数240/280/400与弦长L给出R=L²/(8|h|)+|h|/2，半径与IFC相差小于0.00002毫米；本样本|h|>L/2，IFC取短弧而非直接以h为弧顶高度。符号与截面端部旋转也必须由顶点核对，不能仅核对体积。

顶点检查首先否定了把DB1 frame.normal当作Tekla局部Z的推定：六个IFC的Axis均对应DB1 secondary，IFC Y对应负normal。起拱应位于axis/secondary平面；圆截面直杆的旋转对称性此前掩盖了该轴区别。实现及合成坐标检查按这一独立映射修正，不放宽0.02毫米顶点阈值。

已对已有截面以解析旋转扫掠构建起拱实体，保留原始长度、属性和圆形/多边形表示差异；仅PartCambering影响该路径，普通cambering UDA不改变几何。对无效/冲突属性、自交半径、未验证的轮廓及加工组合明确报告不可构建，不静默返回直杆或未变形切口。保留完整形状变形和加工后起拱为后续缺口。新增正负/大起拱/临界半圆、空心截面、旋转坐标、普通UDA及异常组合的解析几何检查；全部6个真实对象的336个IFC顶点和独立中心线圆/圆截面体积须验证。IFC数量字段仍按未变形长度，不能拿它作为弯曲实体体积基线。主语料1,408项及原有UPE几何检查保持不变。

本轮本地验证：C++17/20各256项、主语料1,408项、OCCT 260项、安装后OCCT消费5项及可选几何语料2项全部通过。全部1,240个实体仍有效，仅上述6个对象的几何发生预期变化。336顶点最大距离约0.0000872毫米，独立体积最大相对误差约1.37e-7。

## 2026-09-27：UPE/IPE公称截面与折线斜接修正（已实现验证，完整几何继续逆向）

完整OCCT与已固定IFC对比发现：无型材目录时，原UPE/IPE代码按高度比例猜测宽度、腹板和翼缘。24根UPE140与1根UPE220体积偏差约-20.56%/-16.86%，却未标注估算。ArcelorMittal Orange Book官方表确认14种UPE（80–400）和18种普通IPE（80–600）的离散尺寸；不能把任意高度代入经验式，也不能将IPE A/O/V/750多质量变体混作普通IPE。

已按确切公称尺寸表替代比例公式，保留已解析项目目录/轮廓的优先级；未列尺寸、变体及未能解释的同名目录记录不虚构截面。固定IFC中的UPE为尖角表示，公称尺寸回退继续生成尖角，并明确说明缺失项目目录与圆角，新增按实际构件ID列出的nominalProfilePartIds，不将标准名称推定当作已读取目录。目录提供圆角时保持其原有几何路径。

已用官方面积/圆角信息交叉检查全部32规格、验证目录覆盖与未知规格拒绝；另增加OCCT完整实体对固定IFC的25个UPE构件体积及顶点表面距离检查，包含已有布尔/平面操作。原有1,408项语料基线不改写，复杂截面、ELD/ARVAL包络及孔/切割等其他几何缺口继续保留。本轮不将尖角回退声明为真实轧制截面的完整圆角几何。

完整顶点检查进一步发现8根UPE140折线梁（22/23点，全部chamferType=0）在转角处约0.097毫米缺口：现有实现把每段直接平头挤出后放入compound，未沿相邻段的角平分面斜接。这不是IFC圆弧离散误差。已按已有路径段方向扩展端部并用共享角平分平面裁切，保留曲线路径八弦近似的既有边界；新增90度/斜角折线的解析体积与转角顶点检查，完整25根IFC检查保持原0.02毫米阈值。

安装验证还发现手工指定OCCT头文件/库的模式虽在构建时绕过包查找，导出配置却仍无条件要求OpenCASCADE包。已记录构建时是否使用包目标，仅该模式在安装消费时调用find_dependency；手工路径模式继续使用其已导出的实际头文件和库，验证Windows安装后独立消费。

本地验证：C++17/20各256项CTest、完整1,408项主语料、OCCT 259项及安装后4项消费测试通过。可选IFC几何语料1项通过，全部25个UPE实体及1,724个顶点符合原阈值。整模型1,240个实体仍全部构建且拓扑有效；72个体积变化中71个缩小与IFC数量差异，另一圆管的4.719755%差异吻合IFC十二边形与解析圆的面积比，保留两种几何表示的区别。该审计不将拓扑有效等同于全模型尺寸正确。

## 2026-09-27：7.30 同工程 IFC 独立属性与几何证据（已实现验证，复杂几何继续逆向）

从公开工程另取得 `out.ifcZIP`，固定压缩包与成员的大小/SHA-256。IFC2X3 导出时间2012-07-10，含1,215个带 `TS_<对象ID>` 标签的梁/板/柱，全部ID和压缩GUID与同工程DB1对应，属于其1,240个实际构件的子集。名称、材质、类别、起终点和存储原点全部吻合；缺少的25个对象不据此判定删除或解析错误。

逐实体遍历IFC属性关系及闭合BREP，在验证父级placement为单位变换后转换顶点。DB1中无布尔/切割/拟合操作、无轮廓且型材为两数字矩形的536个对象，全部八个顶点与DB1原点、轴、截面尺寸和存储长度吻合。该集合按结构条件选取，不按误差挑选；另有180个带操作的简单矩形，其中99个最终包围尺寸与原始截面/长度不同，不能以原始挤出长度代替最终实体长度。其余复杂构件仍需完整OCCT结果与IFC对照。

已加入仅服务固定语料的独立IFC证据读取器，直接读取原始IFC，不从本库输出生成期待值；检查1,215个身份/属性/端点、536个八顶点和明确缺失集合。公开型材字符串可以经导出规范化，不能强制相等；DB1存储长度与IFC最终数量字段也不直接等同。新增一份固定IFC成员及一项证据用例，保持全部既有文件/用例不变。已验证全新下载及压缩包/成员散列，1,408项语料、本地C++17/20各256项CTest与安装后消费通过；五项GUID/材质/原点/顶点/单位变更均被证据检查拒绝，已纳入CI。此工作不新增通用IFC产品API，也不表示复杂几何、切割结果或完整Tekla格式已验证。

## 2026-09-27：7.30 DG图纸语义（已实现部分语义，完整图元继续逆向）

同一公开教学工程的35份DG全部为`Xsteel  7.30`顺序容器，目录后33张表的类型/尺寸完全一致。已审计91个视图（182个坐标基均正交归一）、35个图幅、69条字符串属性；字符串链无断链，视图上下文无歧义，范围最小值不大于最大值。视图/范围采用与7.82相同的40/112/160/232偏移。35份主体中33份type3且模型ID为0，两份type1指向同一DB1构件311724；两份是重复主体，不算两项独立对象证据。

已严格验证33表签名，恢复完整文字链、属性与属性链接、图幅、主体和视图坐标/范围，保持原始字节；字段解释仅覆盖已核实布局。type1作为零件主体、type3作为总布置，尚无7.30装配图证据，type2保留未知分类。7.30没有现代322表，其他数值引用仍处于候选阶段，不伪造空引用意味着没有模型引用，也不自动认定无工程GUID的跨文件归属；明确诊断该缺口。新增35文件逐字节/语义回归、配套DB1主体与图纸目录交叉检查和合成异常/资源预算测试。完整图元、纸面比例/定位与其余引用仍继续逆向。

## 2026-09-27：7.30 编号库与公开历史交叉验证（已实现）

公开教学工程`btscm.fr/.../pechelirejulV1/pechelirejulV1/`提供主DB1、xslib、两份DB2及编号历史。主DB2解压1696字节：`Xsteel  7.30`旧头，9张顺序表，表22有20条76字节系列记录；21个非零零件/装配计数均与同目录Tekla编号历史最大分配号一致。组件DB2只有12字节头，是合法空库。现有读取器只接受现代编号文件头，误拒绝这两份文件。

已精确接受7.30旧头，复用已验证的顺序表边界与76字节系列字段；保留全部未知字段及原始字节，不推断当前对象数量或生命周期。加入真实文件来源/大小/散列、逐系列历史核验、工程聚合，以及旧头/空库/截断/标签/尺寸/重复记录异常测试。DB1 7.30已有几何路径，但逐对象编号尚未映射，本轮不得从DB2计数制造对象位置号或自动认定无GUID工程配对。该模型数量与原7.30基线一致，不能仅因获得公开URL就声称是独立几何样本。

## 2026-09-27：空螺栓位置引用与新增公开训练模型（已实现）

已实现原始位置引用保留与OCCT未构建组索引；新增14份固定文件、24项回归，合计601份文件/1325项语料，默认CTest236项及8项下载器离线测试。已从独立空缓存成功下载两层ZIP并逐文件核对散列。

Tekla支持页链接的Construsoft `Wapening.zip`包含嵌套`oefening W3.zip`，模型产品版本2020SP2、存储8.95。两个螺栓组436963/442032的定义、起终点和placement均存在，但位置数组引用@20为0；现有代码无条件要求轮廓存在，导致整份模型读取失败。计划保留现代原始位置数组引用，允许显式0作为未保存位置数组，并诊断而不推定位置或删除状态；非零断链仍失败。OCCT对没有已存位置的组不生成空compound冒充成功，公开未构建组ID及诊断。新增真实模型、DB2/DBV/组件库聚合检查和合成异常/几何回归。

样本外层495690字节SHA256 `b7ee943ac0026a08794d6732a5f503b3e856f3357b19d18dbfc7630ca8d88185`；下载器须支持逐层固定大小/散列的嵌套ZIP，使用明确客户端标识，验证全新缓存下载。旧有587份文件与1301项用例保持原样，不把该练习的137份未引用钢筋定义当作非空钢筋几何真值。

## 2026-09-27：钢筋实体、参数及关联（实现完成，完整几何继续逆向）

已实现reinforcementDefinitions/reinforcements/reinforcementIdsByFather，新增56项逐文件语料和2项公式参数核验，总计1,301项；587份固定外部文件和原有1,243项基线未变。两种版本组件库各96项参数一致性通过，不视为独立几何真值。默认CTest扩至228项，包含资源预算调整及重复placement拒绝。

资源限制采用可配置的`ModelReadOptions::maxReinforcementArrayValues`（默认16,777,216），同时限制元素展开和块遍历，避免仅按文件尺寸限定而误拒共享长数组的合法模型。调用者可按内存预算调整；阈值拒绝和提高阈值后完整解析均须验证。

56份现代主库/组件库中21份包含kind47实体，共1,447个实例、301份定义；主183/184、组件153/154分别为32/56字节，全部实例都有身份、定义、placement和type47父构件关系。主205/206、组件175/176为120字节double链块与60字节整数链块，已验证每块计数、后继、数值有限性和全部引用。定义name/grade/size位于12/16/20，class位于8；组件公式proNAME/proMATERIAL/proSIZE/proCLASS及同owner参数默认值进一步佐证这些字段。实例@20数组与proBENDING_RADIUS一致，@24数组与proNUMBER_OF_BARS或间距参数对应；模式代码尚未确认，不能把所有数组值都当作间距或数量。

计划公开钢筋定义/实例、类别/名称/材质/尺寸、弯曲半径数组、原始间距或数量数组、已存形状坐标与剩余数组值、父构件反向索引、属性和公式/距离变量引用；保留原始payload与数组引用。形状坐标暂不声明为最终钢筋中心线，尚未恢复多polygon分段、保护层、弯钩、变截面/分布规则和全部世界坐标变换。旧版7.82四份实例表为空，8.44缺独立验证，本轮不扩大到它们。模式、钩参数和实例未知字段不猜测枚举；第三种编号记录仍保留原始内容。加入共享/未使用定义、数组多块/断链/环/计数/非有限值、签名、身份/父关系/placement及资源预算回归；完整既有语料保持不变，新增全现代语料和组件参数交叉核验。

## 2026-09-27：旧版对象编号与显式工程配对

已实现上述7.82编号布局及显式配对选项：四份文件685条记录、43,473条引用（7,410条非零），固定图纸核对4个独立主体及主零件区别；调用者确认旧版基名配对后主库5,192条系列关联，默认无GUID不生成关联。语料新增5项为1,243项，587份固定文件和原有1,238项基线不变；默认CTest197项。尚未恢复的第三种旧记录、有效状态及自动DG工程身份继续保留。

四份7.82样本的主215/216、组件185/186表是64/72字节零件/装配编号记录，描述符8/10项（第一项1，其余0）。共685条记录，主211/组件181保留43,473条对象引用，其中7,410条非零目标全部能连接这两类记录；无缺失身份/目标或重复源。字段相对现代少一个@4槽：start@4、sequence@8，prefix为零件@16/装配@24，各48字节；其余原始字节保留。主217/组件187实际宽96且全空，不能把它当作现代第三种44字节编号记录。配套13份DG中的标记文字对应4个独立主体：两个零件ps4/ps5、两个装配AR2000；装配主零件另为rod1。计划以明确固定文件组合验证这些跨文件文字证据，不把重复图纸计作独立对象真值。

计划复用现有编号API，扩展7.82主库/组件库，保持零序号/特殊范围和未知上下文不猜测。旧DB1/DB2均无GUID：默认仍不建立确定系列关联；增加调用者显式启用的旧版同目录同基名配对选项，并在每条关联标明“显式旧版基名配对”证据，不能绕过现代GUID冲突或版本冲突。异常签名/宽度、断链/重复、原始字节、特殊范围、默认不配对/显式配对及伪造现代冲突均须测试。完整编号生命周期、第三种旧版记录和DG自动工程身份仍未完成。

## 2026-09-26：多格式长期目标（进行中）

现代对象编号已实现：objectNumberingRecords/objectNumberingReferences保留三个布局及对象引用，第三种只公开原始payload；工程入口提供源/目标路径限定的objectNumberingSeriesAssociations。57项新增语料检查，其中187零件/43装配与独立日志一致；实际工程249条系列匹配，20条特殊起始值/零序号记录保留并诊断。587份文件和原有1,181项基线未改动；完整语料为1,238项，默认CTest180项。DB2比较快照、旧版编号、有效状态等仍未完成。

对象编号本轮范围：56份8.95/9.52/9.60主库/组件库中，主322/323（组件286/287）分别有68/76字节零件/装配编号记录，共1,982条；主211/组件181的12字节引用有27,303条指向这些记录。其余非零目标1,447条均指向主324/组件288的44字节未解释记录，不能丢弃或冒充零件号。计划公开三种记录及对象引用，第三种仅保留原始payload；按身份ID检查引用，保留零引用、未引用记录、原始上下文和未知字段。已验证字段为id@0、起始号@8、序号@12、零件前缀@20/装配前缀@28；普通正数范围的派生号=start+sequence-1，序号零或高位特殊起始值不生成确定编号。培训模型269条零件/装配关联中230条与同包编号日志逐GUID完全一致（187零件/43装配）；历史日志不是全部对象当前状态或生命周期证明。工程入口按DB1/DB2同目录同文件基名且GUID一致、精确前缀与数值起始号唯一关联，保留来源路径，不从系列计数猜对象号；缺失/歧义/不一致须诊断。新增真实回归、独立日志验证和异常/跨工程/缺失系列测试。7.82布局、第三种编号、DB2比较快照、变更/有效状态与8.44继续列为缺口。

7.82内嵌位置已实现为Model::partDefinitionPositions：四份文件2,722份定义、5,352条构件定义连接；四项新语料检查保留全部位置槽位及轴向/长度残差对象ID。C++17/20各156项CTest、安装后消费、587份固定文件与1,181项语料回归通过；原有1,177项基线不变。独立旧版位置真值、完整旋转/横向位移、8.44仍有缺口，详见PART_POSITION.zh-CN.md。

7.82内嵌构件位置本轮范围：四份真实主库/组件库共有2,722份380字节构件定义，payload@20..67与现代48字节位置块对应。3,326条无轮廓构件中99条含非零轴向偏移，原先0.005阈值下起点投影匹配，更严格精度审计另发现对象29686约1.335e-6轴向残差；组件库7条零轴向偏移构件的长度残差约0.006至0.071，保留已存长度。计划在独立的partDefinitionPositions中按定义ID保留位置块，不伪造auxiliaryReferenceId，不混用现代位置记录ID。起终轴向偏移@20/@32有几何一致性证据；深度代码/偏移@44/@48、平面代码/偏移@60/@64基于跨版本块布局及代码分布对应，尚缺旧版独立界面真值。剩余六槽保留原始位模式，非零横向/旋转候选不据此命名。需覆盖共享/未引用定义、全部字段、未知代码、非有限值和失败清空；不改变任何已存几何或旧语料基线。

表面处理本轮范围：PSDBIM 7.82组件库的117表（78字节/12字段）有7条kind73记录，118表（292字节/29字段）有6份定义；对应主库表145/146为空。161表type73的7条父构件→表面处理关联均唯一且父对象为普通构件。定义有class@68、name@90、profile@112、material@174、typeName@206、typeCode@268；TS1 / Tile surface 1 与官方表面处理说明、typeCode=3相符。实例有定义、起终点、轮廓、方向引用及已存原点/长度，7条轮廓均可关联。计划先实现7.82的表面处理定义/实例、父构件反向索引、变量/公式和属性关联，保留定义未知头尾及实例22字节尾部，不把处理区域当作结构构件加入parts。型材文本中的PL厚度可派生为optional厚度，并标注来源；颜色、布尔裁剪、完整铺贴/面积/体积和生命周期仍未知。56份现代文件同位置宽度/签名稳定但全为空，因此本轮不宣称现代非空表面处理已支持。新实体须有断链、重复、非有限几何、父关联缺失/歧义和原始字段保留测试；另核对同工程matdb材质名称。旧版构件位置字段的内嵌布局已得到进一步证据，后续单独实现，避免混淆其定义ID与现代辅助ID。

上述7.82表面处理已实现：7个实例、6份定义、7条父关联、32个轮廓点、132个距离变量引用及34个公式目标。工程材质关联7条；587份既有文件、1,177项语料检查、C++17/20各148项CTest和安装后消费通过，原有1,172项基线未修改。现代非空处理、颜色/裁剪/铺贴等仍为缺口，见SURFACE_TREATMENT.zh-CN.md。

配套matdb实测包含6份表面定义引用的全部材质。因此工程入口另提供按源DB1路径/表面对象ID限定的材质目录索引关联；只接受精确且唯一的名称匹配，不在重复名称中任选，也不跨主库/组件库混用数字ID。缺失或歧义仅报告诊断，保留已解析表面对象。

本轮构件位置范围：56份现代主库/组件库的776条52字节辅助记录，确认 payload@4/@16 为起点/终点轴向偏移，@28/@32 为深度代码/偏移，@44/@48 为平面代码/偏移。AUTRA同提交建模源码将主次梁设为Depth.BEHIND、Plane.MIDDLE，30条对应梁均存2/0；柱与基础均存0/0。15,940条无轮廓构件的投影/长度审计中405条有非零轴向偏移，两个重复样本对象的长度残差约0.0076，需保留为待解释证据，不能强制重算几何。@8/@12/@20/@24没有非零样本，@36/@40全部为零，不能命名为横向偏移或旋转。计划公开上述六个有证据字段和剩余六个原始u32，按auxiliaryReferenceId关联；有限值/重复ID/缺失引用检查和独立源码证据测试。数据库origin/length已包含位置调整，不能再应用一次；7.82/8.44及完整位置坐标规则继续保留缺口。

上述位置字段已实现为Model::partPositions，保留共享和未引用记录、未知代码诊断及六个原始槽位。新增56项位置回归、1项同工程源码证据、5项异常/未知代码测试；587个固定文件、1,172项语料回归及C++17/20各126项CTest、安装后消费全部通过，既有1,115项基线未修改。仍不重算完整位置或旋转，详见PART_POSITION.zh-CN.md。

归属修正与现代组件变量本轮范围：已核对 56 份 8.95/9.52/9.60 主库及组件库。8.95 身份偏移 4 全部引用 28 字节类型描述表（主库315/组件库279），偏移8为对象归属上下文；类型描述独立保存，不覆盖旧关联推断的 `Identity::type`。8.95/9.52/9.60 构件偏移8全部引用52字节辅助表（主库245/组件库215），应另存 auxiliaryReferenceId，owner 从身份读取。先验证上述宽度/签名/引用，再修正归属。8.44 缺少本轮独立语料，保持显式限制。随后对已验证现代组件库启用147/156变量表的完整引用签名，核对 owner 列表、公式图和跨版本真实语料；新增回归必须覆盖旧指纹遗漏的归属与辅助引用，而不能只更新数量基线。

实际语料还显示非组件变量作用域：两份 renato 库中 22 个参数和 68 条公式属于 3 个 kind-60 身份对象；9.60 培训组件库中 8 个参数和 14 条公式属于 2 个 kind-60 对象。因此新增通用 variablesByOwner 索引覆盖所有已解码变量，组件自身列表仅表示其 owner 范围；暂不推断 kind-60 的实体含义。三份同提交 AUTRA 对话框文件用于独立核对 15 个参数的名称与归属。

本轮已实现上述归属修正、类型/辅助引用分离和现代组件变量图；60 项新的归属用例全部读取成功。语料扩至585文件/1,115用例，默认CTest121项。字段证据、API和未完成边界见 `OWNERSHIP.zh-CN.md`；下文关于 owner 错误的早期发现已由本轮修正。

复查发现需一并修正的误分类：23份非空样本的关联type4源对象全部为recordKind4（自定义构件），目标包含点、构件、自定义构件和操作构件。旧代码将这些对象错误生成ControlLine。计划在已验证布局中改为customComponentReferences，定义对象提供referenceObjectIds，保留全部目标、不推断非点目标的具体角色；8.44历史布局仍保留待独立验证的限制，真正控制对象另行恢复。

该误分类修正已完成；归属测试现在检查完整type4目标与控制线误生成。C++17/20各121项CTest、安装后消费和全部1,115项语料通过；原有1,054项基线均未修改。

7.82 组件变量/公式本轮范围：xslib ordinal 147（64 字节 / 14 字段）保存 6,838 条距离变量，156（97 字节 / 6 字段）保存 10,450 条公式绑定。前者有名称、标签、两个存储数值、属性/平面 token；第二数值与标志不解释为默认值或可见性。后者有目标对象 ID、原始索引、表达式字符串及目标属性名。162 的 type 58 将每个距离变量连接到两个对象；type 59 的每组引用均恰含一次公式目标，其余是引用对象，跨 owner 引用合法存在。计划公开这些记录、身份/owner 和对象关联，补齐 D1/D5 的对话框核对，并以具体 P6+D5 等绑定核验目标与输入。解析不执行公式，不推断未知参数枚举、平面法向或求解顺序；需覆盖非法引用/签名/非有限数值、重复 ID 与缺失目标关联测试。

上述 7.82 变量/公式读取和关联已实现；四份对话框的 69 个引用全部对应。回归为 582 文件 / 1,054 用例，默认 CTest 105 项；仍不宣称公式求解或几何约束完成。

跨版本复查发现下一项必要修复：28 份现代组件库的 147/156 表保持相同宽度和稳定引用签名，但 8.95 身份 payload 偏移 4 的值不是对象 owner（例如全部参数为 4302，全部距离为 4615，均不在身份 ID 集合）；偏移 8 的 context 对变量可对应组件/自定义定义。9.52 的 owner 在偏移 4，不能共用 8.95 的修正。先核实 8.95 context/assembly/owner 含义及主库差异，修正归属，再将距离/公式读取扩到现代版本，增加真实对象归属证据。现有回归尚未覆盖此错误，README 明确保留限制。

7.82 xslib 定义本轮范围：ordinal 125 的 32 字节 / 9 字段记录保存 ID、kind、未解释分类码、说明字符串引用和名称字符串引用。PSDBIM 库有 40 条（28 条具名，12 条空名称）；名称可与同提交 ComponentCatalog.txt 核对，参数名可与 CustomComponentDialogFiles 的 INP 对话框核对。计划复用 CustomComponentDefinition，保留重复名称及匿名记录，以身份 owner 关联参数/子对象，并检查缺失字符串、身份及重复 ID。两个实际引用填入 referenceIds 前两项，第三项为零（旧格式没有该槽）。连接类记录仍以 Component 返回；分类码、匿名记录用途、历史/当前选择与参数公式求值不作推断。

上述定义映射已实现：40 条定义关联 660 条参数和 2,726 条子对象身份；四份对话框中 67 个参数引用匹配，D1/D5 两个引用保留为缺口。语料扩至 582 文件 / 1,053 用例，默认 CTest 89 项；详细证据、重复/匿名记录及参数边界见 `DB1_782.zh-CN.md`。完整组件求值、现代定义说明、生命周期和类型 39 仍未完成。

DG 7.82 本轮范围：PSDBIM 工程另含 293 份图纸。代表样本显示 `Xsteel! 7.82`、76 字节前导和 47 张现代分节表；每行包含标签、payload、40 字节元数据，表尾为单个零字节。首表的类型目录可能重复整组类型，不应照搬 9.54 的唯一目录约束。计划以全部文件验证容器，并恢复已核实的链式文本、属性、图幅、视图坐标基/范围及旧式数字模型引用。视图 context 在同一文件内可重复，必须以记录 ID 保留所有视图并明确报告 context 歧义。旧图纸缺少已验证的工程 GUID，数字 ID 不能单独充当跨工程关联依据；完整图元、生命周期和纸面变换仍须继续验证。

DG 7.82 本轮已恢复上述局部语义：293 份图纸全部完成容器重建和部分语义读取；59 个主体 ID/类型与配套 DB1 相符。断链文本、退化坐标与重复 context 明确保留，不把局部读取当作完整重绘。语料扩至 577 文件 / 1,052 用例，默认 CTest 81 项；详细证据与未完成项见 `RELATED_FORMATS.zh-CN.md`。

DB1 7.82 本轮范围：两份公开主库均为 228 节，两份组件库为 198 节。已核对点/方向/属性、56 字节构件、380 字节定义、63 字节身份、88 字节装配和 60 字节焊缝定义。完整字段签名为首项 1、其余 0，不能复用 8.95 的引用标志。构件类型来自定义，轮廓直接引用 332 字节点块，首点显式存储；装配成员来自身份负载偏移 20，不能混用 owner 字段。计划恢复这些有证据的字段与关联，并验证主库和组件库的异常引用。

7.82 的螺栓是 type-10 独立构件记录。先公开其定义、原始型材参数、坐标、轮廓和关联构件，不把它伪装成现代螺栓组，也不猜测头部/螺母/孔形状。非板轮廓、旧组件库中的未命名动态记录、其他未知表保留原始数据并报告边界。真实样本的数量和散列仅作为回归基线，后续仍需 Tekla/IFC 导出真值。

用户明确要求将目标扩展为全部关联格式，而非仅 DB1。验收范围包括 DB1/xslib、DB2 编号、DBV 环境与选项、各类目录、Shapes/TEZ、DG 图纸、工程元数据及可验证的跨文件关联。仅发现文件或保留原始容器不计为语义完成；逐格式记录已恢复的字段、版本证据、尚未覆盖的对象与独立真值。

当前优先级为 DB2 与 DG。公开培训模型的 DG 9.54 使用无 magic 的顺序定长表，不能套用现代 DB1 section 扫描。DB2 则使用带表 ID 的顺序表，表后有结束标记，部分表 ID 大于一千万；现有 legacy 扫描会漏掉这些表。先恢复严格容器边界、DB2 编号系列和 DG 字符串/属性/工程 GUID 关联，再继续恢复编号对象分配、视图、尺寸、标记与图元。此次工作不将局部语义阶段标记为全格式完成。

下一阶段推进 DBV：30 份 environment.db 对应 9 表/15 表两套字段签名；60 份 options 库共享 9 表签名。DBV 使用单字节表尾，部分样本的记录标签为 1，不能只按 DB1 的 4/12 标签读取。计划恢复环境类别、属性名称/标签/存储类型、属性与类别关联、整数选项列表，以及 options 库的键名、值类型和成对存储值。属性元数据标志及成对值的默认/当前优先级尚未由差分实验确认，必须保留原值而不臆造有效值；浮点/字符串选项列表目前无非空真实样本。原始数据与未覆盖字段继续公开。

7.82 阶段验证结果：两份主库与两份 xslib 读取通过，新增两个工程聚合用例；共 465 项公开语料回归、72 项默认 CTest。公开独立螺栓记录与连接；18 个 type-39 组件库构件继续列入未处理对象，72 个旧装配引用缺失给出诊断。详细证据见 [DB1_782.zh-CN.md](DB1_782.zh-CN.md)。仍缺独立几何真值、非板轮廓、旧组件定义和螺栓精确几何。

## 2026-09-24：DB1 可靠性与多文件工程入口（历史记录）

DG 后续范围：已在现有 7 份 DG 中核对 19 条 type-260 视图记录的两组正交坐标基，并确认所有 type-322 模型引用的 context 均落到视图。type-269 图纸主体 GUID 在 DB1 中分别对应钢板或装配；3 份 DG 只有零类型占位头，无视图。计划增加视图坐标基、范围/深度和主体关联，通过同包 `.vi` 设置核对一组 6 个范围值，并以 DB1 对象类型与坐标方向交叉验证。比例、纸面原点、缩短和完整投影图元仍需独立真值，不能凭外观猜测完成重绘。

长期方向是离线 Tekla 文件互操作库：独立文件读取 + 模型目录关联。保持现有 TeklaDB1 API 兼容，逐步加入工程级资源索引；不将 DB2、DBV 或 DG 的原始字节读取宣称为完整语义支持。

本轮优先修复现代节 magic 碰撞、schema 字段签名未验证、未知版本误接受、主数据库选择歧义、构件类型/诊断和克隆后的构建测试。网上公开样本以固定提交、下载 URL、SHA-256 和预期结果记录，数据放在仓库外，不把第三方模型纳入源码发行。

验收包括：合成异常输入的 CTest 回归、公开实际模型跨版本读取、安装后独立消费，以及多文件工程清单的确定性与关联诊断。几何近似与尚未支持的对象必须显式报告；历史“完成”记录不代表所有 Tekla 对象及未来布局均已支持。新找到的 7.82、8.95、9.52、9.60 公开样本用于验证或记录边界。

本轮实现：28 个默认 CTest 用例、可选 OCCT 简单体积/网格验收、282 个外部文件的散列清单及 298 个公开语料回归用例。其中 28 份主库和 28 份组件库读取通过，另 2 份 7.82 主库与 2 份组件库明确拒绝语义解析，raw 层可读。所有样本可通过 `tools/corpus.py --download` 重新获取，无第三方模型字节进入仓库。

公开语料确认早期现代表存在 1 字节结束标记；DBV 名称前的整数是名称长度，不是版本。8.95 组件库的未使用倒角槽存在 NaN，不能对未使用槽一律按有效尺寸拒绝：有效坐标仍要求有限值，无法用于构形的活动倒角保留并诊断。`tekla::readProject` 聚合配套目录、raw DB2/DBV、同名配对与图纸清单；`TeklaFormats` 包名与现有 `TeklaDB1` 包共存。

后续验收缺口仍包括：7.82 语义映射、8.44 的独立完整签名复验、现代路径/复杂实体与钢筋专用类型、DB2 编号语义、DG 图纸，以及真实模型的 Tekla/IFC 几何真值。不得用这里的回归通过数替代这些缺口。

## 完成定义

“全部逆向成功”至少同时满足：

1. 能识别并安全遍历已收集的所有 DB1、xslib 和目录容器，未知布局必须给出明确诊断，不能越界猜读。
2. 主模型中的实体、参数、坐标、属性、GUID、所有权、装配和组件关系能够形成稳定的中立对象图。
3. 线性构件、板、折板、轮廓板、钢筋、螺栓、焊缝、切割、布尔体、拟合、控制对象和自定义组件均有明确语义或明确记录为尚未覆盖。
4. `profdb.bin`、`matdb.bin`、`screwdb.db`、`assdb.db`、`profitab.inp`、Shapes XML、ShapeGeometries TEZ 和 `xslib.db1` 均有独立公开解析 API。
5. 不使用 OCC 时返回足以重建几何的参数化描述；启用可选 OCC 模块时能生成拓扑实体并输出可验证网格。
6. 不同版本通过存储版本与表结构签名选择 schema，不依赖单一固定表数量。
7. 每类记录都要有真实样本、边界检查、引用完整性检查和跨版本回归；可构造几何的对象还要与 IFC 或其他真值比较包围盒、体积和位置。

## 已有语料

| 存储版本 | 主模型表数 | xslib 表数 | 来源 |
|---|---:|---:|---|
| 7.30 | 旧式无现代 section magic | 旧式 | 用户真实模型 |
| 8.44 | 286 | 254 | Tekla 2026 SP4 Finland 环境模板 |
| 8.95 | 326 | 290 | Tekla 2026 SP4 USA 环境模板 |
| 9.52 | 356 | 319 | 用户 Tekla 2024 SP3 真实模型 |
| 9.65 | 371 | 327 | Tekla 2026 SP4 多套环境模板 |
| 9.66 | 372 | 328 | Tekla 2026 SP4 USA Latinoamerica 模板 |

环境包和 SHA-256 清单保存在 `D:\其他\DB1\catalogs\Trimble\2026-SP4`。

## 实现状态与验收

### A. 独立核心库（完成）

- 中立命名空间、头文件和 CMake 包。
- zlib 是核心唯一依赖。
- Windows 使用宽字符入口验证中文路径；Linux 使用 UTF-8 路径。
- 7.30 与 9.52 现有样本回归结果不得退化。

### B. 跨版本 schema 映射（已覆盖现有语料）

- 对每张表建立 `(负载尺寸、字段数量、前后邻接、记录标签、引用分布)` 指纹。
- 用序列对齐找到 8.44/8.95/9.65/9.66 与 9.52 的插入、删除和变更表。
- 把解析器中的硬编码 ordinal 收敛为命名表角色，再由版本 schema 解析到实际 ordinal。
- 首先恢复点、坐标基、身份、字符串、构件定义与构件实例，再逐类扩展。

当前结果：

- 9.65 和 9.66 的已使用语义表与 9.52 保持相同 ordinal 和完整表签名；它们分别在末尾追加 15 和 16 张表。核心解析器验证实际使用表的签名，不限制总表数。
- 真实 9.65 模板已成功恢复 7,176 个实体构件、5,529 个操作构件、2,682 组螺栓、7,982 条焊缝、2,359 个装配和 2,272 个组件。
- 8.95 的身份、构件定义、332 字节轮廓、螺栓定义和字符串属性使用独立角色映射；真实模板恢复 1,053 个实体构件、62 个操作构件、395 组螺栓和 192 条焊缝。
- 8.44 使用独立角色映射。旧轮廓块的首点是隐含局部原点，后续分块连续拼接；真实模板恢复 462 个实体构件、107 个操作构件和 112 条焊缝。
- 所有未命名表仍可通过原始容器 API 逐字节读取，避免将未经验证的偏移暴露为伪语义。

### C. 外部目录（完成现有语料）

- `profdb.bin`：版本 9/10、十表结构、参数化型材、固定轮廓、多环与倒角。
- `matdb.bin`：版本 3、六表结构、材料层级、名称、密度和强度属性。
- `screwdb.db`：版本 3、116 字节记录，恢复螺栓尺寸、标准与属性。
- `assdb.db`：版本 3、355 字节记录，恢复螺栓组件及其零件组合。
- `profitab.inp` 与 `.clb`：文本型参数化截面定义及环境覆盖规则。
- Shapes XML/TEZ：名称、GUID、包围盒、几何引用、点、面和内环。
- `pgdb.bin`：五表 Sketch Solver 结果、链式名称、点块、点、圆角和精确截面。
- `TeklaStructuresModel.xml`：版本、环境、语言和模型搜索路径。

### D. xslib 与对象图（完成现有语料）

- 区分模型主库和组件库 schema。
- 恢复动态值、公式、输入参数、组件定义、实例和生成子对象之间的引用。
- 对模型目录、环境目录和模型内覆盖目录建立明确的查找优先级。

17 份 7.30 至 9.66 的 `xslib.db1` 已全部通过语义回归；最大样本恢复 11,297 个参数定义和 520 个自定义组件定义。长公式按链式字符串块完整重组。

### E. 几何（可选后端完成，未知自定义型材显式保留）

- 核心模块公开与 CAD 内核无关的曲线、轮廓、扫掠、放样、布尔、孔和网格描述。
- OCC 模块独立目标、可关闭；不得让 `TopoDS_Shape` 泄漏到核心 API。
- 对每个实体保留原始参数和诊断，失败对象不得静默丢弃。

当前 OCCT 回归：7.30 实体构件 1,240/1,240 构形成功；9.52 实体构件 2,014/2,014 构形成功；8.95 在缺失若干专有自定义型材定义的环境模板中精确构形 438/1,053，其余 615 个构件均保留解析结果、对象 ID 和诊断，不以猜测包围盒冒充精确几何。OCC 三角化由线性/角度误差驱动，不生成固定级数的 LOD。

### F. 验证与发布边界（本次可执行项已完成）

- Windows x64、Linux x86_64、Linux ARM64 核心库构建。
- 截断输入测试，确认读取失败可诊断且不会越界崩溃。
- 大模型峰值内存与解析吞吐基准。
- 安装包导出及独立下游 `find_package` 消费验证。

Windows x64 当前基准使用 14.21 MiB 压缩、49.94 MiB 解压、999,939 条原始记录的 9.65 模型：完整语义解析约 3.1 秒，峰值工作集约 242.6 MiB。优化前为约 15.2 秒和 264.3 MiB；所有权预索引消除了组件与身份表的二次方扫描，关联索引避免重复复制记录，流式 GZIP 解压不再常驻整份压缩输入。结果数量与优化前一致。

核心库已经在 Windows x64、Linux x86_64 和 Linux ARM64 实机编译通过。Windows x64 的可选 OCCT 后端及安装后外部消费项目也已编译、加载并完成真实模型构形/三角化。两台 Linux 构建机目前没有 OCCT 开发包，因此 Linux OCCT 目标仍缺少实机链接验证。

对主模型 DB1、`pgdb.bin`、`matdb.bin` 和 TEZ 各取最大样本，在 15 个边界位置截断，共执行 60 次独立进程读取；全部正常返回成功或可诊断失败，没有异常退出。许可证仍由仓库所有者在首次公开发布前确定，当前不对外发布。

## 仍然存在的边界

- 私有格式没有公开 schema；新 Tekla 版本或尚未收集的对象类型必须以新样本验证后才能命名字段。
- 8.44 样本未包含可验证的螺栓组，因而不把未知表强行命名为螺栓语义。
- 8.95 的旧 schema 没有已验证的 bolt-layer 字段映射；螺栓实体可恢复，孔与具体构件的关联仍通过诊断说明。
- 若模型只引用环境中的专有自定义型材，而模型目录没有相应 `profdb.bin`、`pgdb.bin`、CLB 或 Shape 几何，无法从名称唯一反推真实截面；这类构件不做伪精确构形。
- `DBV` 已恢复环境定义/类别/整数选项及 typed options 成对槽位；仍缺元数据标志、字符串定义值、非整数选项记录、有效值优先级与受控修改真值。详情见 `RELATED_FORMATS.zh-CN.md`。
